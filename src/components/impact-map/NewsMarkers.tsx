// NewsMarkers removed in v2 - clicking behavior now handled by TerrainLabels directly.
// Keeping this file as an empty export to avoid import errors during transition.

export function NewsMarkers() {
  return null;
}
